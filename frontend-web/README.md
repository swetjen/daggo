# DAGGO Frontend

React UI for DAGGO observability.

## Commands

```bash
bun install
bun run build
```

The production build outputs to `frontend-web/dist` and is embedded by the Go API.

## API

Use the generated Virtuous JS client from `frontend-web/api/client.gen.js`.
Do not edit generated files in `frontend-web/api/` manually.
